option(
  'nprpc_root',
  type: 'string',
  value: '',
  description: 'Path to the nprpc source tree or install prefix. Empty: '
    + 'third-party/nprpc, then ../nprpc, then a system libnprpc. Supplies '
    + 'the control plane; without it the compositor still builds.',
)
