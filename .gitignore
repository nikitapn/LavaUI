.DS_Store
/.build
/Packages
/*.xcodeproj
xcuserdata/
DerivedData/
.swiftpm/config/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc
swift-cross-ui
.claude

# Compositor (meson). `.vm` is ~1GB of QEMU disk images built by scripts/vm-init
# and `.cache` is meson's; both are regenerable and neither belongs in git.
compositor/build/
compositor/.vm/
compositor/.cache/
