// C++ interop entry for the canvas engine (no Gtk — safe to enable .Cxx).
// Implementation is compiled into this SPM target (no prebuilt libcanvas.so).
module CxxCanvas {
  header "CanvasEngine.hpp"
  export *
  requires cplusplus
}
