#pragma once

// Shim so the real header (canvas/src/util/result.hpp) stays the single
// source of truth instead of being duplicated (and drifting) here.
#include "../../../../../src/util/result.hpp"
