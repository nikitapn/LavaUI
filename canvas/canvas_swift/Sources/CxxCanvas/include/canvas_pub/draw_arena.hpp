#pragma once

// Shim so the real header (canvas/src/ipc/draw_arena.hpp) stays the single
// source of truth instead of being duplicated (and drifting) here.
#include "../../../../../src/ipc/draw_arena.hpp"
