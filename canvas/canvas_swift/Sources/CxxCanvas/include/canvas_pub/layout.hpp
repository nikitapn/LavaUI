#pragma once

// Shim so the real header (canvas/src/shell/layout.hpp) stays the single
// source of truth instead of being duplicated (and drifting) here.
#include "../../../../../src/shell/layout.hpp"
