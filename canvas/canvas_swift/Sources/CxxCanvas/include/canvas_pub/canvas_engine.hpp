#pragma once

// Shim so the real header (canvas/src/bridge/canvas_engine.hpp) stays the
// single source of truth instead of being duplicated (and drifting) here.
#include "../../../../../src/bridge/canvas_engine.hpp"
